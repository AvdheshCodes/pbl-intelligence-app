# Vercel deployment config
# This tells Vercel: "when someone requests /api/*, proxy it to the backend URL"

{
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
